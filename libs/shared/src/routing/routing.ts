export enum RoutingEnum {
  photos = '/photos',
  files = '/files',
  profiles = '/users/profiles',
  users = '/users',
  subs = '/subs',
  comments = '/comments',
  blogs = '/blogs',
  posts = '/posts',
  admins = '/sa/users',
  auth = '/auth',
  security = '/security/devices',
  base = '/',
}

export enum ApiTagsEnum {
  Posts = 'posts',
  Subs = 'subs',
  Users = 'users',
  Profiles = 'users/profiles',
  Auth = 'auth',
  Security = 'security',
  Admin = 'sa',
  Photos = 'photos',
  Files = 'files',
}
